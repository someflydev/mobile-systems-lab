# Flutter Template Set

Purpose: scaffold minimal Flutter lab from `LAB_SPEC.v2`.

Primary placeholders:
- `{{LAB_ID}}`
- `{{APP_TITLE}}`
- `{{STATE_MODEL}}`
- `{{SENSOR_SUBSCRIPTION}}`
- `{{PERMISSION_REQUEST}}`
- `{{CONFIG_LOAD}}`
- `{{CONFIG_SAVE}}`
- `{{UI_COMPONENTS}}`
- `{{BACKGROUND_BLOCK}}`

Generation rule:
- Use one notifier model for baseline labs.
- Keep service files small and feature-scoped.
