Title: Stage-2D Generator Quality: Runnable-Lite Scaffolds + Render Validation

You are a Cross-Platform Scaffold Engineer.

Goal
- Improve scaffold usefulness without overbuilding: generated outputs should be runnable-lite and free of unresolved placeholders.

Context to honor
- Keep template-driven approach.
- Do not attempt full production project generation for all platforms in one pass.
- Preserve existing template paths.

Objective
- Add generator safeguards and a minimal runnable-lite profile contract per platform.
- Ensure generated lab directories contain the minimum set of files to start platform bootstrap quickly.

Artifacts to create/modify
- Modify: `cli-tools/mobile_systems_lab.py`
- Modify: `templates/kotlin/*` (as needed)
- Modify: `templates/swift/*` (as needed)
- Modify: `templates/flutter/*` (as needed)
- Modify: `templates/react-native/*` (as needed)
- Create: `artifacts/contracts/GENERATOR_OUTPUT.schema.v1.json`
- Create or modify: `artifacts/spec-examples/LAB_99_GENERATOR_SMOKE.spec.v2.json`
- Create or modify: `tests/test_generator_contract.py`
- Modify: `README.md`

Required implementation details
1. Add unresolved-placeholder detection:
   - generation fails if any `{{...}}` token remains in rendered outputs.
2. Add `--profile runnable-lite` (default may remain current) to emit required bootstrap metadata files per platform.
3. Add `--out-root <dir>` so generator validation can run in a scratch directory without touching canonical hand-written labs.
4. Emit a small machine-readable generation manifest in each generated lab folder (for later drift checks).
5. Validate generated output against `artifacts/contracts/GENERATOR_OUTPUT.schema.v1.json` in tests.

Acceptance criteria (must be executable)
- `./cli-tools/mobile-systems-lab generate artifacts/spec-examples/LAB_01_SENSOR_TOGGLE_APP.spec.v2.json --dry-run` exits 0.
- `mkdir -p .tmp/stage2 && ./cli-tools/mobile-systems-lab generate artifacts/spec-examples/LAB_99_GENERATOR_SMOKE.spec.v2.json --force --profile runnable-lite --out-root .tmp/stage2/generated` exits 0 and writes scaffolds without modifying canonical `LAB_01_SENSOR_TOGGLE_APP` directories.
- `! rg -n '{{[A-Z_]+}}' .tmp/stage2/generated` succeeds (no unresolved placeholders in generated output).
- `python3 -m unittest -v tests.test_generator_contract` passes.

Anti-scope (do NOT do)
- Do not generate full Xcode project files from scratch.
- Do not introduce template engines beyond simple string replacement already used.
- Do not add dependency injection or framework-level architecture in generator.

Overengineering guardrails
- Keep generator code in existing CLI module unless file length becomes unmanageable.
- Add only one output contract schema.
- Prefer explicit required-file lists over inferred graph logic.

Risks and mitigations
- Risk: accidental overwrite of hand-written lab files.
  - Mitigation: preserve `--force` gating, add `--out-root` for scratch generation, and clearly log files touched.
- Risk: contract too strict for platform differences.
  - Mitigation: define platform-specific minimums within one shared contract.

Evidence to include in diff summary
- Placeholder detection failure example.
- Generator output manifest example.
- Contract test output.
